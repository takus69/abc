use proconio::input;
use ac_library::Segtree;

fn main() {
    input! {
        n: usize,
        q: usize,
        a: [usize; n],
    }

    let mut Segtree::new(n);
    
    for _ in 0..q {
        input! { x: usize }
        if x == 0
    }
}